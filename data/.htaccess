# Bloqueia todo acesso direto — esta pasta só é lida pelo PHP em api/ e admin/
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
